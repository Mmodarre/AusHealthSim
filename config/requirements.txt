# No external Python packages required
# The application uses sqlcmd and bcp command-line utilities instead of pyodbc

# If you want to use Python for SQL Server interaction, uncomment the following:
pyodbc>=4.0.30

# For environment variable management (optional)
# python-dotenv>=1.0.0

# For command-line argument parsing (optional)
click>=8.0.0

# For dynamic data generation
faker>=18.10.0
numpy>=1.24.0
